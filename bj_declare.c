/**
 * bj_declare.h header file function definitions.
 * Define all functions here, that are declared in bj_declare.h.
 */

int move_counter (int x, int y)
{
    int result_move = x + y;
    return (result_move);
}

int paint_card (int x, int y)
{
    int result_paint_card = x + y;
    return (result_paint_card);
}
